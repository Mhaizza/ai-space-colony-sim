{
  "name": "ai-space-colony-sim",
  "version": "0.1.0",
  "private": true,
  "description": "Colonist AI simulation core — Stage 1 prototype (single-colonist loop)",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "build": "tsc"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
